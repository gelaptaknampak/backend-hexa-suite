# Base image
FROM node:18-alpine

# Set work directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy all files
COPY . .

# Expose port (Back4App uses $PORT)
EXPOSE 3000

# Start the server
CMD ["npm", "start"]
